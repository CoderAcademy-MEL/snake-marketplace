<h1>Cart</h1>

<ul>
  <% @cart.each do |listing| %>
    <li><%= listing.title %> | <%= number_to_currency(listing.deposit / 100.0) %></li>
  <% end %>
</ul>

<%= link_to "Pay Now 💵", "", id: "stripe" %>

<%= javascript_pack_tag "stripe" %>